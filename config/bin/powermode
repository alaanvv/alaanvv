#!/bin/bash

case "$1" in
    performance)
        sudo tlp ac
        ;;
    powersave)
        sudo tlp bat
        ;;
    help)
        echo "Usage: powermode {performance|powersave}"
        ;;
    *)
        echo $(sudo tlp-stat | grep "Mode" | awk '{print $3}')
        ;;
esac
