#!/bin/sh

device=$(lsblk -nrpo NAME,SIZE,TYPE,MOUNTPOINT \
  | awk '$3=="part" && $4!="" {printf "%-20s %s\n", $1, $4}' \
  | fzf \
  | awk '{print $1}')

[ -z "$device" ] && exit 1

sudo umount "$device"
